# DesignKit — references
- Direction: `../../design.md`; wiki: `../../llm_memory/wiki/domains/design-system.md`.
- Template for SwiftUI design layering: `~/Apps/KetoMineralTracker` (`Design/`).
- Apple: SwiftUI `ButtonStyle`/`ViewModifier`, `Color` (sRGB hex), SF Rounded (`.system(design:.rounded)`),
  watchOS `.containerBackground(_:for:.navigation)`.
