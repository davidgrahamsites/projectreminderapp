# context
The watch is the app's heartbeat; a complication keeps the current project glanceable on the watch
face without opening the app. Added in the final phase alongside app icons. Kept minimal and snapshot-
based so it never blocks on persistence.
