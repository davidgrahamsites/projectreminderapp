# DesignKit — agents
Built by the Team Lead in the final design phase (Agent D's brief; executed by Lead after the
background design agent returned idle without output). Owns `Packages/DesignKit/**` and the app
view restyle. Does not touch ReminderKit logic, project.yml, or shared/SCHEMA.md. Follows the
root `agents.md` coordination loop.
