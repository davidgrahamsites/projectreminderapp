# references
Apple WidgetKit (accessory families), `WidgetCenter.reloadAllTimelines()`. Shared snapshot:
`ReminderKit/Store/SharedHeadState.swift`. Embedded via `project.yml` (target dependency, embed: true).
